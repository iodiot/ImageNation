<div id="main">

<% if @user.image_updated_at.nil? %>
	<a class="button" href="#">Load image...</a>

  <%= hidden_field_tag :login, @user.login %>
  <%= file_field_tag :image %>

<% else %>

  <%= image_tag @user.image.url %>

<% end %>

</div>

